<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday Janu</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
            overflow-x: hidden;
        }

        /* ===== HEADER SECTION ===== */
        .header {
            text-align: center;
            margin-bottom: 40px;
            animation: fadeInDown 1s ease;
        }

        .header h1 {
            font-size: 3.5rem;
            color: white;
            text-shadow: 0 0 20px rgba(255, 255, 255, 0.5),
                         0 0 40px rgba(255, 105, 180, 0.6);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
        }

        /* ===== ENVELOPE SECTION ===== */
        .envelope-container {
            perspective: 1500px;
            width: 500px;
            max-width: 90vw;
            margin-bottom: 50px;
        }

        .envelope {
            position: relative;
            width: 100%;
            height: 350px;
            cursor: pointer;
            transform-style: preserve-3d;
            transition: transform 0.6s ease;
        }

        .envelope:hover {
            transform: translateY(-10px) rotateX(5deg);
        }

        .envelope-back {
            position: absolute;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #ec4899 0%, #d946ef 100%);
            border-radius: 10px;
            box-shadow: 0 20px 60px rgba(236, 72, 153, 0.4);
            border: 3px solid rgba(255, 255, 255, 0.3);
        }

        .envelope-front {
            position: absolute;
            width: 100%;
            height: 50%;
            background: linear-gradient(135deg, #f472b6 0%, #ec4899 100%);
            border-radius: 10px 10px 0 0;
            transform-origin: top;
            transition: transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
            z-index: 2;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            border: 3px solid rgba(255, 255, 255, 0.3);
            border-bottom: none;
        }

        .envelope.open .envelope-front {
            transform: rotateX(-180deg);
        }

        .envelope-flap {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 50%;
            overflow: hidden;
        }

        .envelope-flap::before {
            content: '';
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 0;
            height: 0;
            border-left: 250px solid transparent;
            border-right: 250px solid transparent;
            border-top: 175px solid rgba(244, 114, 182, 0.9);
        }

        .heart-icon {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 4rem;
            z-index: 1;
            animation: heartbeat 1.5s ease infinite;
            filter: drop-shadow(0 0 20px rgba(255, 255, 255, 0.8));
        }

        .click-text {
            position: absolute;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%);
            color: white;
            font-size: 1.2rem;
            z-index: 1;
            animation: pulse 2s ease infinite;
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.8);
        }

        /* ===== BIRTHDAY MESSAGE LETTER ===== */
        .letter {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) scale(0.8);
            width: 90%;
            max-width: 450px;
            background: linear-gradient(135deg, rgba(236, 72, 153, 0.95) 0%, rgba(217, 70, 239, 0.95) 100%);
            border-radius: 20px;
            padding: 40px 30px;
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.5);
            opacity: 0;
            transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
            pointer-events: none;
            backdrop-filter: blur(10px);
            border: 3px solid rgba(255, 255, 255, 0.4);
            z-index: 3;
        }

        .envelope.open .letter {
            opacity: 1;
            transform: translate(-50%, -120%) scale(1);
            pointer-events: all;
        }

        .letter-header {
            text-align: center;
            margin-bottom: 25px;
            padding-bottom: 20px;
            border-bottom: 2px solid rgba(255, 255, 255, 0.3);
        }

        .letter-header h2 {
            font-size: 2rem;
            color: white;
            text-shadow: 0 0 20px rgba(255, 255, 255, 0.6);
            margin-bottom: 10px;
        }

        .letter-content {
            color: white;
            line-height: 1.8;
            font-size: 1.1rem;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
        }

        .letter-content p {
            margin-bottom: 15px;
        }

        /* ===== PHOTO GALLERY SECTION ===== */
        .photo-gallery {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
            animation: fadeInUp 1.5s ease;
        }

        .photo-card {
            width: 200px;
            height: 250px;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            transition: all 0.4s ease;
            cursor: pointer;
            border: 4px solid rgba(255, 255, 255, 0.3);
            position: relative;
        }

        .photo-card:hover {
            transform: translateY(-15px) scale(1.05) rotateZ(2deg);
            box-shadow: 0 20px 50px rgba(236, 72, 153, 0.6);
            border-color: rgba(255, 255, 255, 0.8);
        }

        .photo-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.4s ease;
        }

        .photo-card:hover img {
            transform: scale(1.1);
        }

        /* ===== FULLSCREEN MODAL FOR PHOTOS ===== */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            z-index: 1000;
            justify-content: center;
            align-items: center;
            animation: fadeIn 0.3s ease;
        }

        .modal.active {
            display: flex;
        }

        .modal-content {
            max-width: 90%;
            max-height: 90%;
            border-radius: 20px;
            box-shadow: 0 0 50px rgba(236, 72, 153, 0.8);
            animation: zoomIn 0.4s ease;
            border: 5px solid rgba(255, 255, 255, 0.5);
        }

        .modal-close {
            position: absolute;
            top: 30px;
            right: 40px;
            font-size: 3rem;
            color: white;
            cursor: pointer;
            transition: all 0.3s ease;
            text-shadow: 0 0 20px rgba(255, 255, 255, 0.8);
        }

        .modal-close:hover {
            transform: rotate(90deg) scale(1.2);
            color: #ec4899;
        }

        /* ===== ANIMATIONS ===== */
        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes zoomIn {
            from {
                opacity: 0;
                transform: scale(0.5);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        @keyframes heartbeat {
            0%, 100% {
                transform: translate(-50%, -50%) scale(1);
            }
            50% {
                transform: translate(-50%, -50%) scale(1.1);
            }
        }

        @keyframes pulse {
            0%, 100% {
                opacity: 1;
            }
            50% {
                opacity: 0.6;
            }
        }

        @media (max-width: 768px) {
            .header h1 {
                font-size: 2rem;
            }
            .photo-card {
                width: 150px;
                height: 200px;
            }
        }
    </style>
</head>
<body>
    <!-- ========================================
         BIRTHDAY HEADER
         Change the name and emojis here
         ======================================== -->
    <div class="header">
        <h1>🎉 Happy Birthday 💖 ⭐ J A N U ⭐ 🎂</h1>
    </div>

    <!-- ========================================
         ENVELOPE WITH BIRTHDAY MESSAGE
         Click to open the envelope
         ======================================== -->
    <div class="envelope-container">
        <div class="envelope" id="envelope">
            <div class="envelope-back"></div>
            <div class="envelope-flap"></div>
            <div class="envelope-front">
                <div class="heart-icon">💖</div>
                <div class="click-text">✨ Click to Open ✨</div>
            </div>
            
            <!-- ========================================
                 BIRTHDAY MESSAGE INSIDE THE ENVELOPE
                 Edit the message below
                 ======================================== -->
            <div class="letter">
                <div class="letter-header">
                    <h2>Special Birthday Message 💝</h2>
                </div>
                <div class="letter-content">
                    <p>🎈 Wishing you a day filled with love 💕, laughter, and unforgettable memories. May this year bring you endless joy and amazing adventures! 🌟</p>
                    <p>You light up every room you enter and bring happiness to everyone around you. Here's to celebrating YOU today! 🎊</p>
                    <p>May all your dreams come true and may this special day be just as wonderful as you are! 🎁✨</p>
                </div>
            </div>
        </div>
    </div>

    <!-- ========================================
         PHOTO GALLERY
         REPLACE THE IMAGE URLs BELOW WITH YOUR OWN PHOTOS
         Just change the src="..." part in each img tag
         Click on any photo to view it fullscreen!
         ======================================== -->
    <div class="photo-gallery">
        <!-- Photo 1 - Replace this URL -->
        <div class="photo-card" onclick="openModal(this)">
            <img src="https://images.unsplash.com/photo-1530103862676-de8c9debad1d?w=400&h=500&fit=crop" alt="Birthday celebration">
        </div>
        
        <!-- Photo 2 - Replace this URL -->
        <div class="photo-card" onclick="openModal(this)">
            <img src="https://images.unsplash.com/photo-1464349095431-e9a21285b5f3?w=400&h=500&fit=crop" alt="Birthday party">
        </div>
        
        <!-- Photo 3 - Replace this URL -->
        <div class="photo-card" onclick="openModal(this)">
            <img src="https://images.unsplash.com/photo-1558618666-fcd25c85cd5d?w=400&h=500&fit=crop" alt="Birthday fun">
        </div>
        
        <!-- Photo 4 - Replace this URL -->
        <div class="photo-card" onclick="openModal(this)">
            <img src="https://images.unsplash.com/photo-1513151233558-d860c5398176?w=400&h=500&fit=crop" alt="Birthday moments">
        </div>
        
        <!-- 
             TO ADD MORE PHOTOS: Copy the entire photo-card div above and paste it here
             Example:
             <div class="photo-card" onclick="openModal(this)">
                 <img src="YOUR_IMAGE_URL_HERE" alt="Description">
             </div>
        -->
    </div>

    <!-- ========================================
         FULLSCREEN PHOTO MODAL
         This shows when you click on a photo
         Click the X or anywhere outside to close
         ======================================== -->
    <div class="modal" id="photoModal" onclick="closeModal()">
        <span class="modal-close">&times;</span>
        <img class="modal-content" id="modalImage">
    </div>

    <script>
        // ========================================
        // ENVELOPE OPENING FUNCTIONALITY
        // Click the envelope to open/close it
        // ========================================
        const envelope = document.getElementById('envelope');
        
        envelope.addEventListener('click', function() {
            this.classList.toggle('open');
        });

        // ========================================
        // PHOTO MODAL FUNCTIONALITY
        // Opens photo in fullscreen when clicked
        // ========================================
        function openModal(photoCard) {
            const modal = document.getElementById('photoModal');
            const modalImg = document.getElementById('modalImage');
            const img = photoCard.querySelector('img');
            
            modal.classList.add('active');
            modalImg.src = img.src;
            
            // Prevent the click from closing the modal immediately
            event.stopPropagation();
        }

        // ========================================
        // CLOSE MODAL FUNCTIONALITY
        // Closes the fullscreen photo view
        // ========================================
        function closeModal() {
            const modal = document.getElementById('photoModal');
            modal.classList.remove('active');
        }

        // Close modal when pressing Escape key
        document.addEventListener('keydown', function(event) {
            if (event.key === 'Escape') {
                closeModal();
            }
        });
    </script>
</body>
</html>
